#include "stdafx.h"
#include "network.h"
#include "PerceptronLayer.h"

/*
network::network(void)
{
	this->currentSize = 0;
}


void network::setId(PerceptronLayer& p)
{
	this->currentSize++;
	p.setId(this->currentSize);
	
}

void network::add(PerceptronLayer& p)
{
	this->setId(p);
}


network::~network(void)
{
}
*/